# Base URL of the unified Moifone ERP API (same origin ERP uses via VITE_API_BASE_URL).
# Required for live pricing from the database (GET /api/plans). If unset, the site uses data/pricing.js.
NEXT_PUBLIC_API_URL=http://localhost:5000
